# Функционал организации подготовки

## Обзор
В профиль пользователя добавлены три мощных инструмента для организации подготовки к экзаменам:
- 🍅 Pomodoro таймер
- 📅 Календарь расписания
- 📝 Заметки

Все данные сохраняются индивидуально для каждого пользователя в Firebase Realtime Database.

## 1. Pomodoro таймер (`/pomodoro`)

### Функционал:
- Классическая техника Pomodoro (25 минут работы / 5 минут отдыха по умолчанию)
- Автоматический переход между работой и отдыхом
- Длинный перерыв после каждых 4 помодоро
- Анимированный таймер с пульсацией во время работы
- Счетчик завершенных помодоро

### Настройки (сохраняются в Firebase):
- Продолжительность рабочего сеанса (15-60 минут)
- Короткий перерыв (3-15 минут)
- Длинный перерыв (10-30 минут)
- Количество помодоро до длинного перерыва (2-8)

### Firebase структура:
```
users/
  {userId}/
    pomodoro/
      settings/
        - workDuration: 25
        - shortBreak: 5
        - longBreak: 15
        - pomodorosUntilLongBreak: 4
      stats/
        - completed: 42
      history/
        {timestamp}/
          - date: "2024-01-15T10:30:00"
          - duration: 25
```

## 2. Календарь расписания (`/schedule`)

### Функционал:
- Интерактивный календарь с навигацией по месяцам
- Добавление записей на конкретные дни
- Цветовая маркировка (5 цветов на выбор)
- Время, название и описание для каждой записи
- Индикация дней с записями на календаре
- Удаление через swipe влево

### Firebase структура:
```
users/
  {userId}/
    schedules/
      {date}/  # формат: YYYY-MM-DD
        {itemId}/
          - title: "Математика"
          - time: "14:00"
          - description: "Повторить производные"
          - color: 4294901760 (Color value)
```

## 3. Заметки (`/notes`)

### Функционал:
- Сетка разноцветных заметок (2 колонки)
- Поиск по заголовку и содержимому
- Создание и редактирование заметок
- 5 цветов для визуальной организации
- Дата последнего обновления
- Удаление через swipe влево

### Firebase структура:
```
users/
  {userId}/
    notes/
      {noteId}/
        - title: "Формулы тригонометрии"
        - content: "sin²x + cos²x = 1..."
        - color: 4278190335
        - updatedAt: "2024-01-15T15:45:00"
```

## Интеграция

### Маршруты (lib/main.dart):
```dart
'/pomodoro' => PomodoroPage(userId: userId)
'/schedule' => ScheduleCalendarPage(userId: userId)
'/notes' => NotesPage(userId: userId)
```

### Кнопки в профиле (lib/profile.dart):
Раздел "Организация подготовки" содержит три кнопки для навигации к инструментам.

## Дизайн

- Адаптивный дизайн под тему приложения (Provider + ThemeManager)
- Анимации: пульсация таймера, плавные переходы
- Профессиональные карточки с тенями и скруглениями
- Консистентная цветовая палитра с основными темами
- Google Fonts (Nunito) для всех текстов

## Технологии

- **State Management**: setState (локальное состояние)
- **Navigation**: Named routes
- **Database**: Firebase Realtime Database
- **UI**: Material Design + Custom Painters
- **Fonts**: Google Fonts (Nunito)
- **Theme**: Provider-based ThemeManager

## Использование

1. Пользователь заходит в профиль
2. Прокручивает до раздела "Организация подготовки"
3. Выбирает нужный инструмент (Pomodoro/Календарь/Заметки)
4. Все изменения автоматически сохраняются в Firebase
5. Данные доступны при следующем входе
